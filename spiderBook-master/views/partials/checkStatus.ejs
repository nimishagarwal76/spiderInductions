<%
checkStatus = function(id,data)
{
  for (var i=0; i < data.book.length; i++) {
     if (data.book[i].id == id) {
       console.log('why',data.book[i]);
       if(data.book[i].status)
        {
          return data.book[i].status;
        }
        else
        {
          return ;
        }
     }
 }
 return ;
}
%>
